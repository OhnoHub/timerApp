This app uses "elektron" 
--> install elektron via 'npm install electron --save-dev'

Make sure the package.json has '
{
  "name": "my-timer-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  }
}
'
in it.
